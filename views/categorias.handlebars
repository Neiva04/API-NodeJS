<h2>Lista de Categorias: </h2>
{{#each categorias}}
<div class="card mt-4">
    <div class="card-body">
        <h4>{{nome}}</h4>
        <br>
        <a href="/postagens/{{slug}}"><button class="btn btn-primary mt-2">Ver Postagens</button></a>
    </div>
</div>
{{else}}
{{/each}}
<hr>